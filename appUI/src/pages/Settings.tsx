import { FC } from 'react';
import { DefaultLayout } from '../components/DefaultLayout.tsx';

export const Settings: FC = () => (
    <DefaultLayout>
        <h2>Settings</h2>
        <br />
    </DefaultLayout>
);
