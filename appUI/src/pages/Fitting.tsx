import { FC } from 'react';
import { DefaultLayout } from '../components/DefaultLayout.tsx';

export const Fitting: FC = () => (
    <DefaultLayout>
        <h2>Fitting</h2>
        <br />
    </DefaultLayout>
);
