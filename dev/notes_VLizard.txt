how to use python + electron https://github.com/fyears/electron-python-example

read/update api for config (only for user settings)

fetch available models for fitting


spreadsheet.ts jest || vitest

optimize horrible rerenders of UpsertDatasetDialog
	Fullscreen dialogs: maybe disable esc + backdrop click by supplying () => {}, close only by X and Cancel and unmount Component

bump react-query to @Tanstack react-query

significant digits instead of fixed, C UUC

common type for Dialog props

abstract
    { onError: (e) => pushNotification({ message: getApiErrorMessage(e), severity: 'error' }) },

on upsert compound delete old one