návod jak python + electron https://github.com/fyears/electron-python-example

napsat testy na datasets.py, systems.py

popsat v readme konvence UoM [kPa, K]

FITTING QoL
    print residual
    save the fitting results to JSON
    load the fitting results from JSON instead of regression when available
    force recalculation by input params

restrukturalizovat readme do md souborů
    dataset = isobaric table
    system = set of isobaric tables

    FILE STRUCTURE: both input/output data as human readable .tsv files w/ app metadata as .json
